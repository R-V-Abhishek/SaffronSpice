{
  "scripts": {
    "start": "npm run populate-menu && concurrently \"cd backend && npm start\" \"cd frontend && npm start\"",
    "populate-menu": "cd backend && node scripts/populateMenu.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
