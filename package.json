{
  "name": "fwdproject-1",
  "version": "1.0.0",
  "description": "A project for managing restaurant reservations and showcasing the menu.",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run frontend\" \"npm run backend-init\"",
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && nodemon loginserver.js",
    "backend-init": "cd backend && npm run populate-data && nodemon loginserver.js",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\"",
    "build": "cd frontend && npm install && npm run build"
  },
  "author": "Studios Backbanchers",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^6.2.1",
    "nodemon": "^3.1.9",
    "vercel": "^39.3.0"
  }
}
